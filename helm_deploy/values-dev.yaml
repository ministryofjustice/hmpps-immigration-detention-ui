---
# Per environment values which override defaults in hmpps-immigration-detention-ui/values.yaml

generic-service:
  replicaCount: 2

  ingress:
    host: immigration-detention-dev.hmpps.service.justice.gov.uk

  env:
    INGRESS_URL: "https://immigration-detention-dev.hmpps.service.justice.gov.uk"
    HMPPS_AUTH_URL: "https://sign-in-dev.hmpps.service.justice.gov.uk/auth"
    TOKEN_VERIFICATION_API_URL: "https://token-verification-api-dev.prison.service.justice.gov.uk"
    COMPONENT_API_URL: "https://frontend-components-dev.hmpps.service.justice.gov.uk"
    COMMON_COMPONENTS_ENABLED: 'true'
    ENVIRONMENT_NAME: DEV
    AUDIT_ENABLED: "false"

generic-prometheus-alerts:
  alertSeverity: legacy-replacement-alerts-non-prod
